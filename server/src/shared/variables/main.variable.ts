export enum STATUS_NAMES {
  DEFAULT = -1,
  ACTIVE = 1,
  DELETE = 2,
  INACTIVE = 0,
}

export enum USER_TYPES {
  ADMIN = 1,
  USER = 2,
  DEFAULT = -1,
}

export enum BED_POSITION {
  LOWER = 1,
  UPPER = 2,
  DEFAULT = -1,
}
